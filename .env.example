# Infra Pilot Configuration
# Copy to .env and modify as needed

# Port the application listens on (inside container)
PORT=3000

# Data directory for SQLite database (mounted as volume)
DATA_DIR=/data

# Node environment
NODE_ENV=production
